name: Address Testing
on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    services: 
      database: 
        image: minus34/gnafloader:latest
        ports: 
          - 5433:5432
    steps:
      - run: echo "Container running"